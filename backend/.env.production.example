# =============================================================================
# ByteLetters Backend - Production Environment Configuration
# =============================================================================

# Database (PostgreSQL)
# For production, use a managed PostgreSQL service (Supabase, Railway, Neon, AWS RDS)
DATABASE_URL="postgresql://user:password@host:5432/byteletters?sslmode=require"

# JWT Secret - IMPORTANT: Generate a secure random string (min 32 chars)
# Generate with: openssl rand -base64 32
JWT_SECRET="your-secure-jwt-secret-min-32-characters"

# Inbox Domain (for generating user inbox emails)
INBOX_DOMAIN="inbox.byteletters.app"

# Claude API Key (for newsletter processing)
CLAUDE_API_KEY="sk-ant-api..."

# Cloudflare Email Worker (for receiving forwarded newsletters)
CLOUDFLARE_WEBHOOK_SECRET="your-cloudflare-webhook-secret"

# Server Configuration
PORT=3000
NODE_ENV=production

# CORS - Allowed origins (comma-separated)
ALLOWED_ORIGINS="https://byteletters.app,chrome-extension://*"

# Rate Limiting (optional overrides)
RATE_LIMIT_AUTH=10        # Max auth requests per minute
RATE_LIMIT_GENERAL=100    # Max general requests per minute
RATE_LIMIT_FEED=200       # Max feed requests per minute

# Redis (optional, for distributed rate limiting and caching)
# REDIS_URL="redis://localhost:6379"

# Google OAuth (for Chrome Identity verification - optional server-side)
# GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
# GOOGLE_CLIENT_SECRET="your-client-secret"
