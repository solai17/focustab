# ByteLetters Extension Environment Configuration
# Copy this to .env for LOCAL DEVELOPMENT ONLY
# Production builds default to https://antletters-api.onrender.com

# Backend API URL (only needed for local development)
VITE_API_URL=http://localhost:3000
